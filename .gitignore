/target
ui/node_modules
ui/dist
crates/agency-app/gen

.DS_Store

# Claude Code harness worktrees / local state
.claude/

# Agency's own worktrees + local state for this repo. Ignore the directory
# contents but keep .agency/issues/ tracked: the issue files are the tracker,
# so git is what protects them (and lets them merge on agent branches).
.agency/*
!.agency/issues/
